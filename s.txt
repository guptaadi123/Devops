JENKINS_URL/job/merging/build?token=TOKEN_NAME
curl --user "username:password" http://192.168.1.32:8080/job/merging/build?token=redhat


sudo cp -v -r -f * /testing
#!/bin/bash


if sudo docker ps | grep testenv
then
echo "already running"
else
sudo docker run --name testenv -dit -p 8081:80  -v /testing:/usr/local/apache2/htdocs/  httpd
fi



#!/bin/bash


if sudo docker ps | grep testenv
then
sudo docker rm -f testenv
else
echo "container removed"
fi